{
  "name": "@opendesign/rendering",
  "version": "0.3.2",
  "main": "dist",
  "scripts": {
    "build": "yarn run clean-build && yarn run build:ts",
    "build:ts": "tsc --outDir dist --sourceMap --declaration --project ./tsconfig-build.json",
    "clean-build": "rm -rf dist"
  },
  "dependencies": {
    "@avocode/monroe-cli": "0.3.0",
    "cplus": "2.0.1",
    "uuid": "8.3.2"
  },
  "devDependencies": {
    "typescript": "4.4.4"
  }
}
