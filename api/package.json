{
  "name": "@opendesign/api",
  "version": "0.4.2",
  "main": "dist",
  "scripts": {
    "build": "yarn run clean-build && yarn run build:ts",
    "build:ts": "tsc --outDir dist --sourceMap --declaration --project ./tsconfig-build.json",
    "clean-build": "rm -rf dist"
  },
  "devDependencies": {
    "@opendesign/octopus-reader": "0.4.2",
    "open-design-api-types": "0.4.0-rc.1",
    "typescript": "4.4.4",
    "utility-types": "3.10.0"
  },
  "dependencies": {
    "@avocode/cancel-token": "1.4.0",
    "cplus": "2.0.1",
    "form-data": "4.0.0",
    "node-fetch": "3.0.0-beta.9",
    "p-queue": "6.6.2"
  }
}
